import { render, screen } from '@testing-library/react';
import { roundNum, getIconName, getColor, calcPeriod } from "./helpers"

test('Number floor rounding', () => {
  
})

test('Dates are correctly calculated', () => {

})

test('Number comma insertion', () => {

})